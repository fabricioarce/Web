# Project instructions

- Use pnpm.
- Run `pnpm run build` for type checking and production verification.
- Keep public content bilingual in English and Spanish.
- Present scientific research as work in progress unless results are evidenced.
- Never expose Appwrite server keys or email-provider secrets through `PUBLIC_` variables.
- Deploy the static `dist/` output to Appwrite Sites.
